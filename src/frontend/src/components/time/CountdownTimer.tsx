import { useState, useEffect } from 'react';\nimport { AlertCircle, CheckCircle2, Clock } from 'lucide-react';\n\ninterface TimeUntilEvent {\n  event: string;\n  targetTime: Date;\n  days: number;\n  hours: number;\n  minutes: number;\n  seconds: number;\n  isPassed: boolean;\n}\n\nconst DEFAULT_EVENTS = [\n  { name: 'New Year 2027', date: new Date('2027-01-01T00:00:00Z') },\n  { name: 'Summer Solstice', date: new Date('2026-06-20T00:00:00Z') },\n  { name: 'Halloween', date: new Date('2026-10-31T00:00:00Z') },\n];\n\nexport default function CountdownTimer() {\n  const [countdowns, setCountdowns] = useState<TimeUntilEvent[]>([]);\n\n  useEffect(() => {\n    const calculateCountdowns = () => {\n      const now = new Date();\n      const updatedCountdowns = DEFAULT_EVENTS.map((event) => {\n        const target = new Date(event.date);\n        const diff = target.getTime() - now.getTime();\n        const isPassed = diff < 0;\n        const absDiff = Math.abs(diff);\n\n        const days = Math.floor(absDiff / (1000 * 60 * 60 * 24));\n        const hours = Math.floor((absDiff % (1000 * 60 * 60 * 24)) / (1000 * 60 * 60));\n        const minutes = Math.floor((absDiff % (1000 * 60 * 60)) / (1000 * 60));\n        const seconds = Math.floor((absDiff % (1000 * 60)) / 1000);\n\n        return {\n          event: event.name,\n          targetTime: target,\n          days,\n          hours,\n          minutes,\n          seconds,\n          isPassed,\n        };\n      });\n\n      setCountdowns(updatedCountdowns);\n    };\n\n    calculateCountdowns();\n    const interval = setInterval(calculateCountdowns, 1000);\n\n    return () => clearInterval(interval);\n  }, []);\n\n  return (\n    <div className=\"w-full max-w-4xl mx-auto p-6\">\n      {/* Header */}\n      <div className=\"mb-8 flex items-center gap-3\">\n        <div className=\"p-2 bg-primary/10 rounded-lg\">\n          <Clock className=\"h-8 w-8 text-primary\" />\n        </div>\n        <div>\n          <h1 className=\"text-3xl font-bold text-foreground\">Countdown Timer</h1>\n          <p className=\"text-sm text-muted-foreground\">Track time until upcoming events</p>\n        </div>\n      </div>\n\n      {/* Countdown Cards */}\n      <div className=\"grid grid-cols-1 md:grid-cols-2 gap-4\">\n        {countdowns.map((countdown) => (\n          <div\n            key={countdown.event}\n            className={`p-6 rounded-lg border glass-card transition-all ${\n              countdown.isPassed\n                ? 'border-destructive/50 bg-destructive/5'\n                : 'border-primary/50 bg-primary/5 hover:shadow-lg'\n            }`}\n          >\n            {/* Status Icon */}\n            <div className=\"flex items-start justify-between mb-4\">\n              <h2 className=\"text-xl font-bold text-foreground\">{countdown.event}</h2>\n              {countdown.isPassed ? (\n                <AlertCircle className=\"h-5 w-5 text-destructive\" />\n              ) : (\n                <CheckCircle2 className=\"h-5 w-5 text-primary\" />\n              )}\n            </div>\n\n            {/* Status Badge */}\n            <div className=\"mb-4\">\n              <span\n                className={`text-xs font-semibold px-2 py-1 rounded-full ${\n                  countdown.isPassed\n                    ? 'bg-destructive/20 text-destructive'\n                    : 'bg-primary/20 text-primary'\n                }`}\n              >\n                {countdown.isPassed ? 'Event Passed' : 'Upcoming'}\n              </span>\n            </div>\n\n            {/* Countdown Display */}\n            <div className=\"grid grid-cols-4 gap-2\">\n              <div className=\"text-center\">\n                <div className=\"text-2xl font-bold font-mono text-foreground\">\n                  {String(countdown.days).padStart(2, '0')}\n                </div>\n                <div className=\"text-xs text-muted-foreground mt-1\">days</div>\n              </div>\n              <div className=\"text-center\">\n                <div className=\"text-2xl font-bold font-mono text-foreground\">\n                  {String(countdown.hours).padStart(2, '0')}\n                </div>\n                <div className=\"text-xs text-muted-foreground mt-1\">hours</div>\n              </div>\n              <div className=\"text-center\">\n                <div className=\"text-2xl font-bold font-mono text-foreground\">\n                  {String(countdown.minutes).padStart(2, '0')}\n                </div>\n                <div className=\"text-xs text-muted-foreground mt-1\">mins</div>\n              </div>\n              <div className=\"text-center\">\n                <div className=\"text-2xl font-bold font-mono text-foreground\">\n                  {String(countdown.seconds).padStart(2, '0')}\n                </div>\n                <div className=\"text-xs text-muted-foreground mt-1\">secs</div>\n              </div>\n            </div>\n\n            {/* Date Info */}\n            <div className=\"mt-4 pt-4 border-t border-border\">\n              <p className=\"text-xs text-muted-foreground\">\n                Target: {countdown.targetTime.toLocaleDateString('en-US', {\n                  weekday: 'short',\n                  year: 'numeric',\n                  month: 'short',\n                  day: 'numeric',\n                  hour: '2-digit',\n                  minute: '2-digit',\n                })}\n              </p>\n            </div>\n          </div>\n        ))}\n      </div>\n\n      {/* Info */}\n      <div className=\"mt-8 p-4 bg-muted/50 border border-border rounded-lg\">\n        <p className=\"text-xs text-muted-foreground text-center\">\n          ⏳ Timers update live in real-time. All times shown in your local timezone.\n        </p>\n      </div>\n    </div>\n  );\n}
